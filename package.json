{
  "name": "showcase",
  "homepage": "https://adammcneil.github.io/showcase/",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/generate-manifest.js && vite",
    "build": "node scripts/generate-manifest.js && vite build && cp -r images dist/",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
